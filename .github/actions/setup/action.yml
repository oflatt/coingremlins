name: 'Setup'
description: 'Setup racket and othe dependencies'

runs:
  using: "composite"
  steps:
    - name: "Install Racket"
      uses: Bogdanp/setup-racket@v1.9
      with:
        version: "8.10"